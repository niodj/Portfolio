import styled from "styled-components";

export const Navbar = () =>{

    return(
        <Wrapper>Navbar

        </Wrapper>
    )
}

const Wrapper = styled.div`
  border: solid 1px;
  background-color: green;
  width: 200px;
`
